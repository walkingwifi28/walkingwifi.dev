---
interface Props {
    name: string;
}

const { name } = Astro.props;
---

<span
    class="inline-block px-3 py-1 bg-[var(--color-bg-secondary)] rounded-full text-xs text-[var(--color-text-secondary)]"
>
    #{name}
</span>
